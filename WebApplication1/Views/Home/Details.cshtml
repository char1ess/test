@*@using WebApplication1.Models*@
@model WebApplication1.ViewModels.HomeDetailsViewModel
@{
    //Layout = "~/Views/Shared/_Layout.cshtml";
    //ViewBag.Title = "AAADetials";
    var photoPath = "~/images/" + (Model.Student.PhotoPath ?? "3.jpg");
}

@*@{ var student = ViewData["Student"] as Student;}
    <div>
        ID：@student.Id
    </div>
    <div>
        姓名：@student.Name
    </div>
    <div>
        学号：@student.Number
    </div>*@

@*<div>
        ID：@Model.Id
    </div>
    <div>
        姓名：@Model.Name
    </div>
    <div>
        学号：@Model.Number
    </div>*@
<div class="row justify-content-center m-3">
    <div class="col-sm-8">
        <div class="card">
            <div class="card-header">
                <h3>@Model.Student.Name</h3>
            </div>
            <div class="card-body text-center">
                <img src="@photoPath" class="card-img-top" asp-append-version="true"/>
                <h4>ID:@Model.Student.Id</h4>
                <h4>Name:@Model.Student.Name</h4>
                <h4>Number:@Model.Student.Number</h4>
            </div>

            <div class="card-footer text-center">
                <a asp-controller="home" asp-action="index" class="btn btn-primary">返回</a>
                <a href="#" class="btn btn-primary">编辑</a>
                <a href="#" class="btn btn-danger">删除</a>
            </div>
        </div>
    </div>
</div>
